# CloudSafe - Файловый хостинг

Современная веб-платформа для безопасного хранения и управления файлами с удобным интерфейсом и расширенными возможностями.

## 🚀 Основные возможности

### 📁 Управление файлами
- **Загрузка файлов** с поддержкой drag & drop
- **Просмотр и скачивание** файлов
- **Публичные ссылки** для обмена файлами
- **Поиск и фильтрация** по типу файлов
- **Сортировка** по различным параметрам
- **Массовые операции** с файлами

### 🗑️ Корзина (Trash)
- **Временная корзина** для удаленных файлов
- **Восстановление** случайно удаленных файлов
- **Окончательное удаление** из корзины
- **Массовое восстановление** и удаление
- **Автоматическая очистка** корзины
- **Отображение даты удаления** для каждого файла

### 👤 Система пользователей
- **Регистрация и авторизация** пользователей
- **Профиль пользователя** с возможностью редактирования
- **Раздельное хранилище** для каждого пользователя

### 🔐 Административная панель
- **Супер-администратор** - защищенный от удаления и снятия прав
- **Обычные администраторы** - управление пользователями и файлами
- **Статистика системы** - общая информация о файлах и пользователях
- **Управление пользователями** - назначение прав, удаление
- **Управление файлами** - просмотр и удаление файлов всех пользователей

## 🆕 Добавленные функции

### 1. Система супер-администратора

Супер-администратор имеет полный доступ к системе и защищен от:
- Снятия прав администратора
- Удаления аккаунта
- Изменения статуса другими администраторами

#### Способы создания супер-администратора:

**1. Через веб-интерфейс (при первом запуске)**
- При первом запуске приложения автоматически открывается страница `/setup`
- Заполните форму для создания первого супер-администратора
- После создания эта страница становится недоступной

**2. Через переменные окружения**
```bash
export SUPER_ADMIN_USERNAME="admin"
export SUPER_ADMIN_EMAIL="admin@example.com"
export SUPER_ADMIN_PASSWORD="secure_password"
```
При запуске приложения супер-администратор создастся автоматически (если его еще нет).

**3. Через Flask CLI команду**
```bash
flask create-super-admin
```
Интерактивная команда запросит все необходимые данные.

### 2. Drag & Drop загрузка файлов

Улучшенный интерфейс загрузки файлов с поддержкой:
- **Перетаскивания файлов** прямо в браузер
- **Визуальной обратной связи** при перетаскивании
- **Списка выбранных файлов** с возможностью удаления перед загрузкой
- **Отображения размера** каждого файла
- **Иконок для разных типов** файлов
- **Классического выбора** файлов через кнопку

### 3. Временная корзина для удаленных файлов

Полнофункциональная система корзины:
- **Мягкое удаление** - файлы не удаляются физически, а помечаются как удаленные
- **Восстановление** - возможность вернуть случайно удаленные файлы
- **Окончательное удаление** - физическое удаление файлов из корзины
- **Массовые операции** - восстановление и удаление нескольких файлов одновременно
- **Очистка корзины** - удаление всех файлов из корзины одной кнопкой
- **Защита** - удаленные файлы недоступны для скачивания и просмотра
- **Статистика** - удаленные файлы не учитываются в общем размере хранилища

## 📦 Установка

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd file_hosting
```

2. **Установите зависимости**
```bash
pip install -r requirements.txt
```

3. **Настройте переменные окружения (опционально)**
```bash
export SECRET_KEY="your-secret-key"
export DATABASE_URI="sqlite:///instance/database.db"
export SUPER_ADMIN_USERNAME="admin"
export SUPER_ADMIN_EMAIL="admin@example.com"
export SUPER_ADMIN_PASSWORD="secure_password"
```

4. **Запустите приложение**
```bash
python app.py
```

Или используйте Flask CLI:
```bash
flask run
```

## 🔧 Конфигурация

Основные настройки находятся в файле `config.py`:
- `MAX_CONTENT_LENGTH` - максимальный размер загружаемого файла (по умолчанию 1 ГБ)
- `ALLOWED_EXTENSIONS` - разрешенные типы файлов
- `SECRET_KEY` - секретный ключ для сессий
- `DATABASE_URI` - URI базы данных

## 📝 Использование

### Создание супер-администратора

**При первом запуске:**
1. Откройте приложение в браузере
2. Автоматически откроется страница настройки
3. Заполните форму и создайте супер-администратора

**Через CLI:**
```bash
flask create-super-admin
```

**Через переменные окружения:**
Установите переменные `SUPER_ADMIN_USERNAME`, `SUPER_ADMIN_EMAIL`, `SUPER_ADMIN_PASSWORD` перед запуском.

### Работа с файлами

1. **Загрузка файлов:**
   - Перетащите файлы в зону загрузки или выберите через кнопку
   - Добавьте описание (опционально)
   - Нажмите "Загрузить"

2. **Удаление файлов:**
   - Файлы перемещаются в корзину, а не удаляются окончательно
   - Восстановите их из корзины при необходимости

3. **Корзина:**
   - Доступна через меню навигации
   - Восстановите или окончательно удалите файлы
   - Используйте массовые операции для нескольких файлов

### Административная панель

Доступна по адресу `/admin` для пользователей с правами администратора:
- Просмотр статистики системы
- Управление пользователями
- Управление файлами всех пользователей

## 🗄️ База данных

Приложение использует SQLite по умолчанию. База данных автоматически создается при первом запуске в папке `instance/`.

Миграции выполняются автоматически при запуске приложения:
- Добавление поля `is_admin` для пользователей
- Добавление поля `is_super_admin` для пользователей
- Добавление полей `is_deleted` и `deleted_at` для файлов

## 🔒 Безопасность

- Пароли хранятся в хешированном виде
- Защита от CSRF атак
- Проверка прав доступа к файлам
- Супер-администратор защищен от удаления
- Удаленные файлы недоступны через публичные ссылки

## 📂 Структура проекта

```
file_hosting/
├── app.py                 # Основное приложение
├── models.py              # Модели базы данных
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
├── instance/              # База данных
├── uploads/               # Загруженные файлы
├── users/                 # Хранилище пользователей
├── static/                # Статические файлы (CSS)
└── templates/             # HTML шаблоны
    ├── admin/             # Шаблоны админ-панели
    ├── base.html          # Базовый шаблон
    ├── index.html         # Главная страница
    ├── login.html         # Страница входа
    ├── register.html      # Регистрация
    ├── upload.html        # Загрузка файлов
    ├── files.html         # Список файлов
    ├── trash.html         # Корзина
    └── ...
```

## 🎨 Технологии

- **Backend:** Flask (Python)
- **Database:** SQLite (SQLAlchemy ORM)
- **Frontend:** Bootstrap 5, Material Symbols
- **Authentication:** Flask-Login

