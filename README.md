# 📁 CloudSafe - Облачное хранилище файлов

Веб-приложение для хостинга файлов, построенное на Flask. Позволяет пользователям загружать, хранить, скачивать и управлять своими файлами через удобный веб-интерфейс.

## ✨ Возможности

- 🔐 **Аутентификация пользователей** - Регистрация и авторизация с безопасным хранением паролей
- 📤 **Множественная загрузка файлов** - Загрузка нескольких файлов одновременно
- 📥 **Скачивание файлов** - Безопасное скачивание с отслеживанием количества загрузок
- 🖼️ **Предпросмотр изображений** - Просмотр изображений прямо в браузере
- 🔗 **Публичное расшаривание** - Создание публичных ссылок для файлов с безопасными токенами
- 🗑️ **Удаление файлов** - Удаление отдельных файлов или массовое удаление
- 📊 **Умный дашборд** - Поиск, фильтрация и сортировка файлов с пагинацией
- 🔍 **Поиск и фильтрация** - Поиск по имени файла, фильтрация по типу (изображения, видео, аудио, архивы)
- 📈 **Сортировка** - Сортировка по имени, размеру или дате загрузки
- 👤 **Профиль пользователя** - Редактирование профиля, изменение пароля, email и имени пользователя
- 🔒 **Приватность** - Каждый пользователь видит только свои файлы
- 📈 **Статистика** - Отображение общей статистики на главной странице
- 👨‍💼 **CLI команды** - Управление пользователями через командную строку

## 🛠️ Технологии

- **Python 3.8+**
- **Flask** - Веб-фреймворк
- **Flask-SQLAlchemy** - ORM для работы с базой данных
- **Flask-Login** - Управление сессиями пользователей
- **SQLite** - База данных (можно переключить на PostgreSQL/MySQL)
- **Werkzeug** - Утилиты для работы с файлами и безопасностью
- **Jinja2** - Шаблонизатор

## 📋 Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/file_hosting.git
cd file_hosting
```

### 2. Создание виртуального окружения

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка конфигурации

Создайте файл `.env` в корневой директории проекта (опционально):

```env
SECRET_KEY=your-secret-key-here
DATABASE_URI=sqlite:///instance/database.db
```

Если не указать `SECRET_KEY`, будет использован ключ по умолчанию (не рекомендуется для production).

### 5. Инициализация базы данных

База данных создается автоматически при первом запуске приложения.

### 6. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

## 👨‍💻 Использование

### Создание пользователя через CLI

Для создания пользователя используйте CLI команду:

```bash
flask create-admin
```

Вам будет предложено ввести:
- Имя пользователя
- Email
- Пароль

**Примечание:** Команда называется `create-admin`, но создает обычного пользователя. Для создания администратора может потребоваться дополнительная настройка ролей в будущих версиях.

### Основные маршруты

**Публичные маршруты:**
- `/` - Главная страница со статистикой
- `/register` - Регистрация нового пользователя
- `/login` - Вход в систему
- `/share/<token>` - Просмотр расшаренного файла (публичный доступ)
- `/share/<token>/download` - Скачивание расшаренного файла (публичный доступ)

**Защищенные маршруты (требуется авторизация):**
- `/logout` - Выход из системы
- `/dashboard` - Личный кабинет пользователя с поиском, фильтрацией и сортировкой
- `/upload` - Загрузка файлов (поддержка множественной загрузки)
- `/file/<file_id>` - Просмотр информации о файле
- `/preview/<file_id>` - Предпросмотр изображений
- `/download/<file_id>` - Скачивание файла
- `/delete/<file_id>` - Удаление файла (POST запрос)
- `/files/bulk-delete` - Массовое удаление файлов (POST запрос)
- `/file/<file_id>/share` - Включение/отключение публичного расшаривания (POST запрос)
- `/profile` - Профиль пользователя (редактирование данных и пароля)

### Поддерживаемые форматы файлов

- Текстовые: `.txt`, `.pdf`, `.csv`
- Изображения: `.png`, `.jpg`, `.jpeg`, `.gif`
- Архивы: `.zip`, `.rar`, `.7z`, `.tar`, `.gz`
- Аудио/Видео: `.mp3`, `.mp4`
- Документы: `.doc`, `.docx`, `.ppt`, `.pptx`, `.xls`, `.xlsx`

Максимальный размер файла: **1 GB** (можно изменить в `config.py`)

### Функции дашборда

- **Поиск** - Поиск файлов по имени (регистронезависимый)
- **Фильтрация** - Фильтрация по типу файла:
  - Изображения (image/*)
  - Видео (video/*)
  - Аудио (audio/*)
  - Текстовые файлы (text/*)
  - Архивы (zip, rar, 7z, tar и др.)
  - Другие типы MIME
- **Сортировка** - Сортировка по:
  - Имени файла (A-Z, Z-A)
  - Размеру (по возрастанию, по убыванию)
  - Дате загрузки (новые сначала, старые сначала)
- **Пагинация** - 20 файлов на страницу
- **Массовые операции** - Выбор нескольких файлов для удаления или удаление всех файлов

## 📁 Структура проекта

```
file_hosting/
│
├── app.py                 # Главный файл приложения
├── config.py              # Конфигурация приложения
├── models.py              # Модели базы данных (User, File)
├── requirements.txt       # Зависимости проекта
├── README.md             # Документация
│
├── instance/             # Директория для базы данных
│   └── database.db       # SQLite база данных
│
├── uploads/              # Директория для загруженных файлов
│
├── users/                # Директории пользователей
│
├── static/               # Статические файлы (CSS, JS, изображения)
│   └── style.css        # Стили приложения
│
└── templates/            # HTML шаблоны
    ├── base.html        # Базовый шаблон
    ├── index.html       # Главная страница
    ├── login.html       # Страница входа
    ├── register.html    # Страница регистрации
    ├── files.html       # Личный кабинет (дашборд с файлами)
    ├── upload.html      # Страница загрузки файлов
    ├── download.html    # Страница информации о файле
    ├── share.html       # Страница просмотра расшаренного файла
    ├── profile.html     # Страница профиля пользователя
    └── error.html       # Страница ошибок
```

## ⚙️ Конфигурация

Основные настройки находятся в файле `config.py`:

- `SECRET_KEY` - Секретный ключ для сессий (можно задать через переменную окружения)
- `DATABASE_URI` - URI базы данных (по умолчанию SQLite)
- `MAX_CONTENT_LENGTH` - Максимальный размер загружаемого файла (по умолчанию 1 GB)
- `ALLOWED_EXTENSIONS` - Разрешенные расширения файлов
- `SITE_NAME` - Название сайта

### Использование переменных окружения

Для production рекомендуется использовать переменные окружения:

```bash
export SECRET_KEY=your-secret-key-here
export DATABASE_URI=postgresql://user:password@localhost/dbname
```

## 🔒 Безопасность

- Пароли хранятся в хешированном виде (Werkzeug)
- Защита от CSRF атак
- Проверка прав доступа к файлам (пользователь может видеть только свои файлы)
- Безопасное именование файлов (secure_filename)
- Уникальные имена файлов для предотвращения конфликтов
- Безопасные токены для публичного расшаривания (secrets.token_urlsafe)
- Валидация типов файлов перед загрузкой
- Ограничение размера файлов

## 🚀 Деплой

### Production

Для production рекомендуется:

1. Использовать production WSGI сервер (Gunicorn, uWSGI)
2. Настроить обратный прокси (Nginx)
3. Использовать PostgreSQL вместо SQLite
4. Настроить HTTPS
5. Использовать переменные окружения для секретных ключей
6. Настроить резервное копирование базы данных

### Пример запуска с Gunicorn

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```


